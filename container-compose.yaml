services:
  ocr:
    image: localhost/textnode:latest
    container_name: ocr_service
    ports:
      - 8002:8001
    restart: unless-stopped
    volumes:
      - weights:/root/.EasyOCR/model

volumes:
  weights:
    name: easyocr_models